name: deploy

on:
  workflow_run:
    workflows: ["tests"]
    types:
      - completed
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script_stop: true
          script: |
            cd /var/www/lordoftongs
            git pull origin main
            composer install --no-dev --optimize-autoloader
            npm ci && npm run build
            php artisan migrate --force
            php artisan config:cache && php artisan route:cache && php artisan view:cache
            sudo chown -R ubuntu:www-data /var/www/lordoftongs
            sudo chmod -R g+r /var/www/lordoftongs
            sudo chmod -R g+rw /var/www/lordoftongs/storage /var/www/lordoftongs/bootstrap/cache
            sudo systemctl reload php8.4-fpm
            sudo systemctl restart lordoftongs-worker
